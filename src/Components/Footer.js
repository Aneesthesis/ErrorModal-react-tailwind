import React from "react";

function Footer() {
  let text = `</> with ❤️ by anees`;

  return (
    <footer className="absolute w-full bottom-0 text-center text-white">
      {text}
    </footer>
  );
}

export default Footer;
